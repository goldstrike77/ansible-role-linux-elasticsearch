ELASTICSEARCH_EXPORTER_OPTS='--es.timeout=60s --es.uri={% if elastic_auth | bool and elastic_https | bool %}https{% else %}http{% endif %}://{% if elastic_auth %}elastic:{{ elastic_pass | urlencode }}@{% endif %}{{ ansible_default_ipv4.address }}:{{ elastic_port_rest }} --web.listen-address {{ ansible_default_ipv4.address }}:{{ elastic_port_exporter }} --{{ " --".join(vars["elastic_exporter_collector"]) }} --es.ssl-skip-verify --log.level="warn" --log.format="logfmt"'